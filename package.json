{
  "name": "pomodoro",
  "private": true,
  "description": "Internship Journal Tracker – root for monorepo scripts and API serverless deploy",
  "type": "module",
  "scripts": {
    "dev": "npm run dev:server & npm run dev:client",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "cd client && npm run build",
    "build:api": "echo 'API uses serverless; no build step'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^8.2.1",
    "groq-sdk": "^0.9.0",
    "helmet": "^8.0.0",
    "morgan": "^1.10.1",
    "pdfkit": "^0.16.0",
    "serverless-http": "^3.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
